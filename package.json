{
  "name": "c-o-visualization",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "start": "node server/server.js",
    "check": "node --check server/server.js && node --check server/app.js && node --check server/config.js && node --check scripts/build.js && node --check scripts/dev.js && node --check scripts/client-build.js",
    "test": "node --test server/tests/orderStore.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "read-excel-file": "^9.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
